describe('Authentication Server Test Suite', () => {
  describe('POST /auth/register', () => {
    it.todo('creates and returns the new user')
    it.todo('sends a verification email')
  })

  describe('POST /auth/login', () => {
    it.todo('returns the tokens')
  })
})
